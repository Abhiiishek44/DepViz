# Code Graph MVP

Electron + React + TypeScript starter for the codebase analysis MVP.

## Development

- `npm run dev` starts the Vite dev server and Electron app.
- `npm run build` builds main/preload and the renderer.
- `npm run start` runs Electron against the built output.
